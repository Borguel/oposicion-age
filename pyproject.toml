[tool.black]
line-length = 100

[tool.ruff]
line-length = 100

[tool.ruff.lint]
# Selección por defecto de ruff (errores de sintaxis + pyflakes): detecta
# imports/variables sin usar y errores reales sin imponer un estilo de
# código nuevo sobre las ~130 páginas del proyecto de golpe.
select = ["E4", "E7", "E9", "F"]
