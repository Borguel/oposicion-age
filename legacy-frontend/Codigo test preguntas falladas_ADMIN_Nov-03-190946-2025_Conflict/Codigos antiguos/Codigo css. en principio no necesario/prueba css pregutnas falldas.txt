:root {
  --color-primary: #4caf50;
  --color-primary-dark: #45a047;
  --color-secondary: #2196F3;
  --color-secondary-dark: #1e88e5;
  --color-accent: #FF9800;
  --color-accent-dark: #fb8c00;
  --color-danger: #ef5350;
  --color-danger-dark: #e53935;
  --color-text: #333;
  --color-bg: #f5f6fa;
  --color-card: #ffffff;
  --color-border: #e1e1e1;
  --shadow: 0 4px 20px rgba(0,0,0,0.08);
  --transition: all 0.3s ease;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Inter', sans-serif;
  font-size: 16px;
  line-height: 1.6;
  background: var(--color-bg);
  color: var(--color-text);
  padding: 20px;
}

.container {
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
}

.card {
  background: var(--color-card);
  border-radius: 16px;
  box-shadow: var(--shadow);
  padding: 30px;
  margin-bottom: 30px;
  transition: var(--transition);
}

h1, h2, h3 {
  color: #2c3e50;
  margin-bottom: 20px;
  text-align: center;
}

/* --- BOTONES DE NAVEGACIÓN --- */
.botones-navegacion-test {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
  margin-top: 32px;
  margin-bottom: 12px;
}

.botones-navegacion-test button {
  min-width: 135px;
  padding: 14px 24px;
  font-size: 1.08em;
  border-radius: 8px;
  border: none;
  margin: 0;
  font-weight: bold;
  text-transform: uppercase;
  cursor: pointer;
  transition: background 0.18s, box-shadow 0.17s, transform 0.13s;
  box-shadow: 0 2px 6px rgba(40, 80, 70, 0.08);
  letter-spacing: 0.03em;
  display: flex;
  align-items: center;
  justify-content: center;
}

#btn-anterior {
  background: #2196F3;
}

#btn-anterior:hover,
#btn-anterior:focus {
  background: #1769aa;
}

#btn-desmarcar {
  background: #707070;
}

#btn-desmarcar:hover,
#btn-desmarcar:focus {
  background: #444;
}

.btn-naranja,
#btn-siguiente {
  background: #ffa726;
}

.btn-naranja:hover, .btn-naranja:focus,
#btn-siguiente:hover, #btn-siguiente:focus {
  background: #fb8c00;
}

@media (max-width: 600px) {
  .botones-navegacion-test {
    flex-direction: column;
    align-items: stretch;
    gap: 10px;
  }
  .botones-navegacion-test button {
    width: 100%;
    min-width: unset;
    font-size: 1em;
    padding: 14px 0;
  }
}

/* --- RESTO DE TUS ESTILOS --- */
#titulo-formulario {
  font-size: 2rem;
  text-align: center;
  margin-bottom: 30px;
  color: #2c3e50;
}

.test-type-container {
  display: grid;
  grid-template-columns: repeat(1, 1fr);
  gap: 20px;
  margin: 20px 0;
}

.test-type-card {
  border: 2px solid #e0e0e0;
  border-radius: 12px;
  padding: 20px;
  text-align: center;
  cursor: pointer;
  transition: var(--transition);
  background: white;
  position: relative;
  overflow: hidden;
}

.test-type-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 25px rgba(0,0,0,0.1);
}

.test-type-card.selected {
  border-color: var(--color-primary);
  background: #f0fdf4;
  box-shadow: 0 5px 15px rgba(76, 175, 80, 0.2);
}

.test-type-card .icon {
  font-size: 2.5rem;
  margin-bottom: 15px;
  color: var(--color-primary);
}

.test-type-card .title {
  font-weight: 600;
  margin-bottom: 8px;
}

.test-type-card .description {
  font-size: 0.9rem;
  color: #666;
}

/* Añade aquí el resto de tus estilos personalizados para inputs, cards, temporizadores, resultados, etc. */

