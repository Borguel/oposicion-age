<div class="chat-wrapper">
  <button id="toggle-sidebar" title="Mostrar historial">☰</button>

  <div class="chat-sidebar hidden" id="chat-sidebar">
    <h3>Conversaciones</h3>
    <ul id="chat-history">
      <li class="active">Sesión actual</li>
      <li>Constitución Española</li>
      <li>Temario AGE</li>
      <li>Esquemas</li>
    </ul>
  </div>

  <div class="chat-main">
    <div class="chat-header">
      <img src="https://randomuser.me/api/portraits/women/44.jpg" alt="Asistente" class="chat-avatar">
      <div>
        <div class="chat-title">Asistente IA</div>
        <div class="chat-status">🟢 En línea</div>
      </div>
    </div>

    <div id="chat-messages" class="chat-messages"></div>

    <div class="chat-input-container">
      <textarea id="chat-input" placeholder="Escribe tu mensaje..." rows="1"></textarea>
      <button id="chat-send" title="Enviar">
        <img src="https://cdn-icons-png.flaticon.com/512/2099/2099085.png" />
      </button>
    </div>
  </div>
</div>

<style>
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  height: 100%;
  font-family: 'Segoe UI', sans-serif;
  background: #f3f4f6;
}

.chat-wrapper {
  display: flex;
  height: 100dvh; /* Usa 100% del alto real del dispositivo */
  overflow: hidden;
  position: relative;
}

#toggle-sidebar {
  position: absolute;
  top: 10px;
  left: 10px;
  font-size: 24px;
  background: none;
  border: none;
  z-index: 200;
  cursor: pointer;
}

.chat-sidebar {
  width: 250px;
  background: #f9fafb;
  padding: 20px;
  border-right: 1px solid #e5e7eb;
  overflow-y: auto;
  transition: transform 0.3s ease;
  height: 100%;
  position: absolute;
  top: 0;
  left: 0;
  z-index: 150;
}

.chat-sidebar.hidden {
  transform: translateX(-100%);
}

.chat-sidebar h3 {
  margin-bottom: 10px;
  font-size: 18px;
  color: #1f2937;
}

#chat-history {
  list-style: none;
}

#chat-history li {
  padding: 8px 12px;
  border-radius: 6px;
  margin-bottom: 6px;
  background: #e5e7eb;
  cursor: pointer;
  font-size: 14px;
}

#chat-history li:hover {
  background: #d1d5db;
}

#chat-history li.active {
  background: #cbd5e1;
  font-weight: bold;
}

.chat-main {
  display: flex;
  flex-direction: column;
  flex-grow: 1;
  height: 100%;
  width: 100%;
  background: #fff;
}

.chat-header {
  height: 50px;
  background: #f1f5f9;
  border-bottom: 1px solid #e5e7eb;
  display: flex;
  align-items: center;
  padding: 0 20px 0 50px;
  position: relative;
}

.chat-avatar {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  margin-right: 10px;
}

.chat-title {
  font-weight: bold;
  font-size: 14px;
  color: #1f2937;
}

.chat-status {
  font-size: 12px;
  color: #10b981;
}

.chat-messages {
  flex-grow: 1;
  padding: 10px 20px;
  overflow-y: auto;
  background: #ffffff;
}

.chat-message {
  margin-bottom: 15px;
  max-width: 75%;
  padding: 12px 16px;
  border-radius: 12px;
  line-height: 1.5;
  white-space: pre-wrap;
}

.chat-message.user {
  background: #e5e7eb;
  align-self: flex-end;
  text-align: right;
  color: #1f2937;
  margin-left: auto;
}

.chat-message.bot {
  background: #f3f4f6;
  align-self: flex-start;
  text-align: left;
  color: #1f2937;
  margin-right: auto;
}

.chat-input-container {
  display: flex;
  padding: 12px 20px;
  border-top: 1px solid #e5e7eb;
  background: #f9fafb;
  align-items: center;
}

#chat-input {
  flex-grow: 1;
  resize: none;
  border: 1px solid #d1d5db;
  border-radius: 20px;
  padding: 10px 15px;
  font-size: 14px;
  min-height: 40px;
  max-height: 150px;
  outline: none;
  overflow-y: auto;
}

#chat-send {
  background: #000;
  border: none;
  border-radius: 50%;
  margin-left: 10px;
  padding: 10px;
  cursor: pointer;
  height: 42px;
  width: 42px;
}

#chat-send img {
  width: 18px;
  height: 18px;
  filter: invert(1);
}
</style>

<script>
const input = document.getElementById("chat-input");
const sendBtn = document.getElementById("chat-send");
const chatMessages = document.getElementById("chat-messages");
const toggleSidebar = document.getElementById("toggle-sidebar");
const chatSidebar = document.getElementById("chat-sidebar");

toggleSidebar.addEventListener("click", () => {
  chatSidebar.classList.toggle("hidden");
});

function enviarMensaje() {
  const texto = input.value.trim();
  if (!texto) return;

  agregarMensaje("user", texto);
  input.value = "";
  input.style.height = "auto"; // reset textarea height

  fetch("https://oposicion-age.onrender.com/chat", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ mensaje: texto, temas: [] })
  })
    .then(res => res.json())
    .then(data => {
      agregarMensaje("bot", data.respuesta || "Sin respuesta.");
    })
    .catch(() => {
      agregarMensaje("bot", "❌ Error al conectar.");
    });
}

function agregarMensaje(tipo, texto) {
  const div = document.createElement("div");
  div.className = `chat-message ${tipo}`;
  div.textContent = texto;
  chatMessages.appendChild(div);
  chatMessages.scrollTop = chatMessages.scrollHeight;
}

sendBtn.addEventListener("click", enviarMensaje);

input.addEventListener("keydown", function (e) {
  if (e.key === "Enter" && !e.shiftKey) {
    e.preventDefault();
    enviarMensaje();
  } else {
    setTimeout(() => {
      input.style.height = "auto";
      input.style.height = input.scrollHeight + "px";
    }, 0);
  }
});
</script>