/* ----------- CHAT IA VISUAL PREMIUM (CSS LIMPIO) ----------- */

/* General escritorio */
.chat-container-pro {
  display: flex;
  max-width: 950px;
  margin: 34px auto 0;
  border: 1.5px solid #e2e8f0;
  border-radius: 16px;
  overflow: hidden;
  font-family: 'Segoe UI', 'Inter', sans-serif;
  background: #fff;
  box-shadow: 0 6px 30px rgba(0,0,0,0.10);
  height: 80vh;
  min-height: 600px;
  position: relative;
}
.chat-sidebar-pro {
  width: 260px;
  background: #f7f8fa;
  padding: 28px 18px 0 22px;
  border-right: 1.5px solid #ececec;
  box-shadow: 2px 0 10px #0001;
  display: flex;
  flex-direction: column;
  height: 100%;
  position: relative;
  transition: left 0.28s;
  z-index: 10;
}
#chat-history {
  list-style: none;
  padding: 0;
  margin: 0;
  flex: 1 1 auto;
  overflow-y: auto;
}
/* ---- ESCRITORIO ---- */
.sidebar-bottom-actions {
  flex-shrink: 0;
  background: linear-gradient(to top, #f7f8fa 80%, transparent);
  width: 100%;
  text-align: center;
  padding-bottom: 24px; /* Para escritorio */
  position: sticky;
  bottom: 0;
  z-index: 2;
}
.btn-salir-sidebar {
  width: 92%;
  margin: 12px auto 0 auto;
  padding: 15px 0;
  background: #e54848;
  color: #fff;
  border: none;
  border-radius: 20px;
  font-size: 18px;
  font-weight: 700;
  box-shadow: 0 2px 12px #faacac77;
  cursor: pointer;
  transition: background 0.18s;
  display: block !important;
}
.btn-salir-sidebar:hover { background: #b62020; }
.input-area-pro { padding-bottom: 20px !important; }

/* ----------- MÓVIL Y TABLET ----------- */
@media (max-width: 950px) {
  .chat-container-pro {
    max-width: 100vw !important;
    width: 100vw !important;
    min-width: 0 !important;
    margin: 0 !important;
    border-radius: 0 !important;
    box-shadow: none !important;
    height: 100vh !important;
    position: fixed !important;
    left: 0; top: 0;
    z-index: 9999; /* Para que esté por encima de la página */
    background: #fff !important;
    display: flex !important;
    flex-direction: row !important;
  }
  body, html {
    overflow: hidden !important;
    height: 100vh !important;
  }
  .chat-sidebar-pro {
    position: fixed;
    left: -320px; top: 0; height: 100vh;
    border-radius: 0 18px 18px 0;
    box-shadow: 9px 0 35px #0003;
    width: 220px;
    z-index: 99; transition: left 0.27s;
    background: #f7f8fa;
    /* AQUÍ VIENE EL TRUCO: */
    display: flex !important;
    flex-direction: column !important;
    padding: 0 0 0 0 !important;
  }
  .chat-sidebar-pro.visible { left: 0; }
  h3 {
    margin-top: 24px;
    margin-bottom: 16px;
    font-size: 17px;
    padding-left: 2px;
    flex-shrink: 0;
  }
  #chat-history {
    flex: 1 1 auto !important;
    overflow-y: auto !important;
    padding-bottom: 74px !important; /* Espacio para el botón */
    margin-bottom: 0 !important;
  }
  .sidebar-bottom-actions {
    /* TRUCO: position absolute dentro del sidebar, para forzar que SIEMPRE se vea */
    position: absolute !important;
    bottom: 0; left: 0;
    width: 100% !important;
    padding-bottom: 14px !important;
    background: linear-gradient(to top, #f7f8fa 80%, transparent);
    text-align: center;
    z-index: 5;
  }
  .btn-salir-sidebar { display: block !important; }
  .btn-menu-historial { display: block !important; }
  .chat-box-pro { width: 100vw !important; }
  .input-area-pro { padding-bottom: 120px !important; }
}