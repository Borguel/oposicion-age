# Dependencias solo para desarrollo/tests: python -m pytest usa este fichero
# (pip install -r requirements-dev.txt), no el de producción.
-r requirements.txt
pytest==9.1.1
ruff==0.16.3
black==26.5.1
bandit[toml]==1.9.4
pip-audit==2.10.1
